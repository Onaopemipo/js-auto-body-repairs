---
deployment:
  tasks:
    - export DEPLOYPATH=$HOME/js-auto-body-preview/
    - /bin/mkdir -p $DEPLOYPATH
    - /bin/rsync -a --delete --exclude='.git' --exclude='node_modules' --exclude='.next' ./ $DEPLOYPATH
